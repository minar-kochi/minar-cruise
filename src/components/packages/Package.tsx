import BookingCard from "./PackageCard";
import GalleryCard from "./GalleryCard";
import PackageCard from "./PackageCard";

const Package = () => {
  return (
    <main>
      <GalleryCard/>
      <PackageCard/>
    </main>
  );
};

export default Package;
