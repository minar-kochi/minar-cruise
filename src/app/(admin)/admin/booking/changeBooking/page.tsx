import React from "react";

/**
 * get schedule id
 * @returns 
 */

export default function Page() {
  /** 
   *  fetch the schedule id with specific data
   * if not schedule id then return to booking or show that the id hasn't found.
   */
  return (
    <div>
      {/* pass the value to the OfflineBookingForm component and set the from value to be used as the default form value. */}
      <h1 className="font-bold text-3xl">changeBooking</h1>
    </div>
  );
}
