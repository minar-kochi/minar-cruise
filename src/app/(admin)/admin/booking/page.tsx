import ScheduleSelectors from "@/components/admin/dashboard/_Schedule/ScheduleSelectors";

const page = () => {
  return (
    <div>
      <ScheduleSelectors />
    </div>
  );
};

export default page;
